{
  "name": "ai-business-assistants",
  "version": "0.1.0",
  "private": true,
  "description": "Control API for the AI Business Assistants contour.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/telegram-routing.js && node --check src/telegram-intake.js && node --check src/telegram-reply.js",
    "test:routing": "node --test test/telegram-routing.test.js",
    "test:intake": "node --test test/telegram-intake.test.js",
    "test:reply": "node --test test/telegram-reply.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
